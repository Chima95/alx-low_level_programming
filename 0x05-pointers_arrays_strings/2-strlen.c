#include "main.h"

/**
 * swap_int - swaps the values of two integers
 *            using two input parameters
 *
 * @a: input parameter 1
 * @b: input parameter 2
 *
 * Return: Nothing
 */

int _strlen(char *s)
{
int u;

u = 0;
while(s[u] != '\0')
{
u++;
}return (u);
}
